services:
  - type: web
    name: webhook-receiver
    runtime: node
    plan: free
    buildCommand: yarn install --frozen-lockfile
    startCommand: node app.js
    envVars:
      - key: NODE_ENV
        value: production
